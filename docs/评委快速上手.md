# 评委快速上手(NovaPilot 免安装体验包)

本包已包含运行所需的一切(代码、构建产物、依赖),**无需联网、无需安装任何东西**,
双击即可启动。这是项目的核心工程原则:默认离线确定性运行,现场断网也能完整演示。

## 启动

- **macOS**:双击「启动.command」(首次若提示安全拦截,右键 → 打开 → 打开)
- **Windows**:双击「启动.bat」
- **Linux/其他**:终端执行  PORT=3210 ./node_modules/.bin/next start

启动后浏览器打开 **http://localhost:3210**(首次加载约 1-2 秒)。

## 四个页面

| 页面 | 地址 | 演示什么 |
| --- | --- | --- |
| 客户咨询 | / | 四角色工作台、项目事实、流式回答、科学决策卡(证据芯片可点击跳转) |
| 专家工作台 | /expert | 转接队列、SLA、证据审查排除、待决策勾选、批准出候选知识 |
| 知识进化 | /knowledge | 四道门禁、NovaBench 金标回归、一键回滚、审计轨迹 |
| 运营评测 | /operations | 真实趋势、五开关退化矩阵、质量事件闭环、运行历史、金标明细 |

## 推荐演示动线(约 5 分钟,与 demo视频.mp4 一致)

1. 咨询台:选「实验室 PI」→ 左侧项目事实填 样本数 24 / DV200 62 / RNA 投入 20 →
   提问"请为这批FFPE样本给出建库与测序方案" → 观察思考过程与流式输出 → 正式决策卡
   → 点击证据芯片跳转证据。
2. 专家工作台:认领案例 → 排除一条不适证据 → 勾选待决策项 → 批准 → 生成候选知识。
3. 知识进化:Owner 批准 → 运行评测 → 人工批准 → 开始灰度 → 一键回滚 → 重新灰度。
4. 运营评测:运行 NovaBench(两次出现真实趋势)→ 打开「引用失效」退化开关 → 关闭事件
   (填证据)→ 历史回看 → 展开 9 条金标明细。

## 说明

- 数据存于本地 .data/ 目录(自动生成),删除该目录即回到初始演示状态。
- 演示会话免登录;四角色通过页面角色切换器体验。
- 需要源码/文档/QA 脚本的完整版见 GitHub: github.com/lyz-wave/NovaPilot。
- 本包为 macOS/arm64 构建的即开即用版;其他平台请用源码包(仓库 README 快速开始)。
