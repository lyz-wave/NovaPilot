# 《NovaPilot 量化指标体系 v1.1》达成度自评 — 人工论证

> **此文件由人工维护**，数字部分由 `npm run score:render` 从 `docs/metric-scorecard.json`
> 自动生成并写入 `docs/指标体系达成度自评.md`。修改数字请改 scorecard.json，修改论证请改此文件。

---

## 方法论说明

评估日期 2026-09-11（S0 复评，revision="S0"）。被评对象：本仓库 v3.2.1（511 单测 / 42 文件全绿）。

评估方式：逐条核对指标体系文档的每个指标**是否有真实数据源 + 聚合 + 看板可见**，而不是「是否在文档里被提到」。判定分三档：**已落地**（score=1.0）/ **代理口径**（score≤0.7，isProxy=true）/ **未落地**（score<0.5，有 gap 说明）。

机器可验证版本：`docs/metric-scorecard.json`（39 项逐条评分 + 小计）；断言测试：`src/server/eval/scorecard.test.ts`（15 条断言，含 5 条探针一致性断言）。

---

## 修订历史

| 版本 | 日期 | 分数 | 说明 |
|---|---|---|---|
| W0 | 2026-09-06 | 94.5 | 初版自评；W1~W5 交付后记账滞后 |
| S0 | 2026-09-11 | 98.5 | 补记 761994c/960e9fb 已落地的 9 项；建立探针-计分-断言三段式反馈链路 |

---

## 结构性完成情况

四处埋点缺口全部闭合、第 10 节六对护栏成对上板、P0/P1/P2 告警路径落地。
S0 复评后剩余 1.5 分缺口集中在两条**设计性代理口径**：

1. **§3 角色分布**（0.7 分）：咨询者按会话计、其余角色按活动条数计，分母不同源。S1 引入 `role-activity.ts` 统一分母后转真口径。
2. **§7 灰度期问题率**（0.7 分）：`quality_events` 缺 `candidate_id` 列，无法逐候选归因。S1 补迁移后转真口径。

两条代理口径都有 `proxyNote` 说明，且 scorecard.test.ts 断言 8 确保代理分永远 ≤ 0.7。

---

## A 维度论证（埋点缺口闭合）

文档第 12 节自述「缺口四处」，全部闭合。A=25/25 满分。

| 编号 | 文档要求 | 实现 | 判定 |
|---|---|---|---|
| A | 引用号反查审计 | `citation_audits` + `auditCitations()` | ✅ |
| B | 复核队列 judge 预审 | `review_samples` + 两级判定 | ✅ |
| C | 专家办结候选知识标记 | `case_closures` | ✅ |
| D | 决策卡采纳事件（sync 通道豁免） | `adoption_events` + 3 通道 | ✅ |

S0 复评将 A 维从 24 升至 25：`sync` 通道豁免理由（飞书双写是系统行为非用户采纳）已纳入口径纪律说明，第 13 节无失守。

---

## B 维度论证（护栏对成对上板）

六对护栏在 `guardrailBoard()` 同源同窗计算，看板 `PairRow` 类型强制激励↔护栏成对。

B=19/20 剩余 1 分：「引用核实合规率」台账已落地（S0 复评从 0.7 升 1.0），但样本量仍在扩充阶段（当前 4 条，目标 ≥6）。S1 扩充后无悬念满分。

---

## E 维度论证（口径纪律）

第 13 节 8 条反游戏化条款逐条核对，无失守。E=15/15。
