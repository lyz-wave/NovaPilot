# NovaPilot 前端优化说明

> 本目录是 `NovaPilot_完整项目_20260719` 的**前端优化副本**。
> 原目录任何文件均未改动;本目录只修改前端(src/app、src/components),后端/领域/测试逻辑与原版完全一致。

## 一、评审结论

原前端**需要优化**。设计语言(纸张 × 科研记录本)本身很好,但存在四类实际问题:

| 类别 | 问题 |
| --- | --- |
| 可读性 | 大量 7–9px 字号(标签、芯片、元数据),低于可读与 WCAG 要求 |
| 跨平台 | 字体栈只覆盖 macOS(Avenir/PingFang/Iowan),Windows/Linux 回退到 Arial/宋体 |
| 布局 hack | 上下文环 + 模型徽章用 `position:fixed; right:190px` 悬浮在顶栏上,依赖魔法数字且易重叠 |
| 可达性 | Tab 无方向键导航;下拉/弹窗无 Escape 与外部点击关闭;弹窗无 dialog 语义与焦点管理;若干文字对比度 4.26:1(< 4.5:1);小图标按钮命中区不足 40px |

## 二、优化内容(改动均在副本内)

### 设计系统(globals.css 重写)
- **跨平台字体栈**:拉丁(Segoe UI/Helvetica/Georgia)+ 中文(微软雅黑/苹方/思源/宋体)三层回退,三平台观感一致
- **字号重构**:最小字号 7px → 10px,正文 13–14px,标签 10–11px,并重排信息层级
- **对比度修复**:`--ink-faint` #64738a → #5d6c83,全部正文对比度 ≥ 4.5:1(axe 验证)
- **圆角同心刻度**:`--r-sm/md/lg/pill` 设计 token,外层 = 内层 + 内边距
- **分层阴影**:`--shadow-sm/shadow/lg` 三档,替代原单一重阴影
- **动效规范**:全部 `transition: all` 简写改为显式属性 + 150ms ease-out;按钮统一按压反馈(scale .96–.98)、悬停轻抬
- **纸张质感**:纯 CSS SVG 噪点纹理 + 页头径向光晕(零网络请求)
- **细节**:标题 `text-wrap: balance`、正文 `pretty`;数字 `tabular-nums` 防跳动;`-webkit-font-smoothing`;统一细滚动条;信号点脉冲动画

### 布局与组件
| 组件 | 优化 |
| --- | --- |
| layout / global-nav(新增) | 导航改为客户端组件,当前页 `aria-current="page"` + 墨色下划线;新增 `viewport` 导出(themeColor) |
| nova-workspace / consultation-thread | **移除 fixed 徽章 hack**,上下文环 + 模型徽章作为 headTools 并入咨询页头部;输入框随内容自动增高(上限 190px);空输入禁用发送;场景条 `aria-pressed`;编排进度 `role="status" aria-live` |
| decision-card-panel | 完整 Tab 语义:`aria-controls/tabpanel`、方向键/Home/End 导航、roving tabindex;空状态增加虚线占位卡;清理重复 import |
| project-rail | 会话下拉支持 Escape 关闭、点击外部关闭、焦点归还;`aria-haspopup/expanded` 修正;菜单定位修正 |
| model-settings | 弹窗 `role="dialog" aria-modal`、Escape 关闭、打开时焦点入弹窗、关闭后焦点归还、背景滚动锁定;消息 `role="status"` |
| expert / knowledge / operations | 提示条 `aria-live` 状态播报;案例队列 `aria-current`;真实/模拟退化切换 `aria-pressed`;雷达图 `aria-hidden` |

### 响应式
- 桌面三栏 250px / 1fr / 460px;≤1180px 收窄;≤940px 隐藏侧栏、决策卡吸顶、底部导航;≤720px 单列,头部工具换行。4 个页面在 1440px 与 390px 下均无横向溢出(已实测)。

## 三、验证结果(全部通过)

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | ✅ 0 错误 |
| `npm test`(vitest) | ✅ 17 文件 / 135 测试全通过 |
| `npm run build` | ✅ 构建成功 |
| Playwright 冒烟 | ✅ 4 页面 HTTP 200、无控制台错误、桌面/移动端无横向溢出 |
| 交互实测 | ✅ 发送按钮空输入禁用 / 输入启用;下拉 Escape 关闭;弹窗 focus 归还;Tab 方向键切换 |
| axe 无障碍扫描 | ✅ 4 页面 0 严重/关键违规(修复后) |

## 四、如何运行

`node_modules` 通过软链指向原项目(未复制 592MB 依赖),在相邻目录下直接可用:

```bash
cd NovaPilot_完整项目_20260719_前端优化版
npm run dev          # 开发模式(默认 3000,若与原项目冲突请用 PORT=3210)
npm run build && PORT=3210 npm start   # 生产模式
```

原项目若仍在 3000 端口运行,请用 `PORT=3210 npm run dev` 避免冲突。

**QA 冒烟脚本**:`.smoke-check.cjs`(需先启动服务并修改其中的 BASE 地址),运行 `node .smoke-check.cjs`。

## 五、全按钮点击路径审计(2026-08-15)

对 4 个页面的**全部按钮**做了静态逻辑追踪 + Playwright 动态点击验证,共 **58 项断言全部通过**,全程 0 控制台错误。覆盖:

- 咨询页 25+ 按钮:发送(空输入禁用/输入启用)、示例插入、三种演示场景、追问块两个按钮、授权报价、导出下载、反馈、Tab×3、语言×3、清空、新建/切换/重命名/删除会话、上下文环、自动压缩、模型弹窗全部操作、事实确认
- 专家页:案例切换、认领、退回、批准、复选框
- 知识页:Owner 批准 → NovaBench → 人工批准 → 5% 灰度四级门禁链(含禁用联动与拦截逻辑)
- 运营页:运行 NovaBench、真实/模拟退化切换、证据抽屉

**审计发现并修复的 4 个真实逻辑问题**(均已修复并回归验证):

| # | 问题 | 修复 |
| --- | --- | --- |
| 1 | 流式响应未完成时切换/清空/新建会话,旧流结果会混入当前视图(异步竞态) | `nova-workspace` 增加 `streamConvRef` 归属守卫 |
| 2 | 进行中可并发触发新一轮(场景条/追问/确认事实按钮未禁) | `run()`/`confirmFacts()` 增加 `isPending` 守卫 |
| 3 | 授权/反馈/专家请求状态跨卡片残留(新卡复用旧卡按钮态) | `decision-card-panel` 在卡片变化时复位三态 |
| 4 | 知识晋级本地先标记"已通过"再请求,失败时 UI 误标 | `knowledge-evolution` 改为先持久化成功再同步本地 |

**保留的已知小事项(演示设计,非缺陷)**:确认事实会以标准问句作为用户消息发送;"补充 DV200 = 62%" 按钮文案与侧栏可编辑事实之间以默认值 62 为准;删除会话/模型配置无二次确认弹窗;专家页"在线 4 人"为静态演示文案。

## 六、流式输出(2026-08-15)

科研咨询的输出已改为**流式展示**:

- 传输层本就是 SSE(编排检查点实时推送);本次把"整段一次性显示"的回复正文与决策卡摘要改为**逐字渐进渲染 + 闪烁光标**(约 190 字符/秒),完成后光标消失、文本定格
- **为什么不直接逐 token 推送**:咨询输出要经 Actor→Critic 双智能体证据审查后才允许放出,后端逐 token 流式会泄露未经审查的文本;客户端渐进渲染既保留了审查门禁,又实现了打字机观感,且离线(确定性)与在线(真实 LLM)两种模式行为一致
- 支持 `prefers-reduced-motion`:关闭动效的用户直接看到全文
- 细节:仅当用户停在底部附近时才跟随滚动(不打断回看历史);流式期间关闭线程容器 aria-live(避免读屏高频播报,进度轨迹保留独立 role="status")
- 实现:`src/components/streaming-text.tsx`(新增)、`consultation-thread.tsx`、`nova-workspace.tsx`、`globals.css`
- 验证:`.streaming-check.cjs` 9 项断言全部通过(渐进增长/光标/完成态/摘要与面板一致/reduced-motion),并回归重跑 58 项按钮审计全部通过

## 七、思考中折叠条 + 流速调节(2026-08-15)

- **放慢流式速度**:约 190 字符/秒 → 约 100 字符/秒(`streaming-text.tsx` 中 `TICK_MS=20 / CHUNK=2`),边读边生成更自然
- **思考中折叠条**:助手消息顶部新增"⬇️ 思考中"(进行中,带转圈)按钮,点击箭头展开编排思考过程(接收事实 → 场景判定 → 风险评估 → 证据检索 → 草拟建议 → 证据审查 → 风险门 → 形成决策卡);完成后自动变为"已思考 N 步"绿色胶囊,仍可随时展开回顾
- 实现:回合对象新增可选 `checkpoints` 字段(`domain/consultation-journey.ts`,服务端不感知);`nova-workspace` 用 `progressRef` 在流结束时快照本轮全部检查点并挂到助手回合上;`consultation-thread` 新增 `ThinkingBlock` 组件(默认折叠、aria-expanded/aria-controls、箭头旋转);历史回合(切换会话后加载)无该字段时不显示思考条
- 验证:`.streaming-check.cjs` 更新为 11 项断言(思考中/已思考 N 步/点击展开/步骤数与内容/reduced-motion/渐进增长),全部通过;58 项按钮审计回归全部通过,0 控制台错误

## 八、对齐诺禾致源官网口径(2026-08-15)

对照官方站(https://cn.novogene.com)医学转录组测序产品页与送样建议页,将演示中的客户可见数字对齐为官方口径:

| 位置 | 原演示口径 | 官方口径 | 状态 |
| --- | --- | --- | --- |
| 项目周期(侧栏/决策卡/三语) | 6–8 周 | ≤30 样本 **18 天** | ✅ 已改 |
| 测序深度/质量 | 仅 50M reads | PE150 · **6–12 Gb/样本 · Q20>90% / Q30≥85%** | ✅ 已补入主方案与知识库 |
| 服务名称 | NovaPilot FFPE RNA 一体化服务 | **医学转录组测序** | ✅ 已改 |
| 服务适配说明 | 泛化描述 | 对齐官方(0.4 μg 起始、Illumina、Q30 ≥ 85%) | ✅ 已改 |
| 起始量口径 | 只写 ≥10 ng 低输入边界 | 官方标准路线 **400 ng(0.4 μg)**;10–100 ng 属低输入场景 | ✅ 新增官方规格证据 NV-SOP-MED-001(知识库 + 决策卡证据列表) |
| 送样要求 | 无 | 石蜡切片 **12 张 · 5–10 μm · >25 mm²**;肿瘤样本 RNAlater/液氮速冻、干冰运输 | ✅ 写入官方规格证据 |

**刻意保留**的演示设定(与官网无关,属演示叙事):内部 SOP "DV200 <40% 试建库" vs PMID 35361992 的 30–40% 灰区冲突(证据冲突场景的核心故事,源自文献);"≥10 ng 自动推荐边界"是低输入路线的判定阈值,官方标准路线 400 ng 已作为证据并列呈现;预算 ¥35,000–55,000(官网未公开报价,保留"以授权报价为准")。

**未采用**的官网内容:客服电话 400-658-1585、邮箱 service@novogene.com、科创板代码 688315 等真实渠道信息——演示产品不宜冒用官方客服渠道;诺禾云平台 Novomagic2.0 与演示的知识进化/运营评测定位不同,不强行对标。

验证:`.align-check.cjs` 6 项断言全部通过(侧栏周期/决策卡周期/服务适配标题与说明/主方案深度口径/官方规格证据),58 项按钮审计与 135 项单元测试回归全部通过。

## 九、命题补强:四角色工作台 / 四大场景 / 业务 KPI(2026-08-16)

针对命题符合性评审发现的三处最大缺口,补齐:

1. **四角色工作台**(命题要求 1):咨询页头部新增角色切换器(PI / 博士后 / 研究生 / 企业研发,aria-pressed 分段控件)。四角色共享同一项目事实与证据链,差异化体现在:角色注记条、欢迎语(对话策略)、侧栏专属模块——PI「决策约束」、博后「参数与文献」、研究生「三步引导」(①研究问题→②样本条件→③方案清单,随事实确认动态打勾)、企业「批次与 SLA / 数据边界」。
2. **四大核心场景**(命题要求 1):场景条从 3 个扩到 5 个——方案设计、**数据分析(新增)**、**论文支持(新增)**、条件缺失、证据冲突。新场景走完整真实管线(意图识别→检索→Actor-Critic→决策卡),新增两条受治理 SOP 证据 NV-SOP-ANALYSIS-001(DESeq2/edgeR、GO/KEGG 富集、FDR<0.05)与 NV-SOP-PAPER-001(方法学记录、火山图/热图规范、统计量标注),卡片推荐与证据 Tab 自动绑定新证据。
3. **业务 KPI 预期上屏**(命题要求 5):运营看板新增「业务指标预期」卡(8 项,竞赛目标区间):AI 安全自助解决率 55–65%、首次响应时间缩短 ≥90%、人工平均处理时长下降 35–45%、七日内复开率下降 ≥25%、引用有效率 ≥98%、高风险转接召回率 ≥95%(硬门禁)、满意度 +8–12pp、新客户触达 +30–50%;明确标注"上线前经基线测量与试点校准",与 NovaBench 实测区隔。

实现:领域层新增 `Scenario` 两个枚举与 `ClientRole` 类型/`CLIENT_ROLES` 元数据;知识库新增 2 条 SOP;工作台/线程/侧栏/运营看板组件与 CSS 相应扩展。

验证:`.capability-check.cjs` 16 项断言全部通过(角色切换与各角色侧栏/欢迎语、两大新场景卡片内容与证据绑定、KPI 卡 8 项数值、移动端角色切换与无溢出);对齐验证 6/6、流式验证 11/11、按钮审计 58/58 全量回归通过,0 控制台错误。

### 9.1 角色工作台视觉归组(2026-08-16)

评审反馈"角色元素分散"后重构:把散落在中栏头部(切换器)、中栏标题下(注记)、左栏底部(专属模块)的角色 UI **收进一个集中的"角色工作台"面板**(咨询页标题下方,role-bar):左侧角色标签页(role=tablist,aria-selected)+ 右侧该角色专属模块(chips/三步引导)+ 注记一行。左栏回归纯共享信息(项目事实/隐私/说明),中栏头部只留工具类控件(上下文环/模型徽章/语言/清空)。另加一处真实视角差异:**博士后视角下新决策卡默认打开"证据"标签页**(文献对比优先),其余角色默认"方案"。移动端角色栏自动转为上下布局(标签横向滚动)。

验证:`.capability-check.cjs` 更新为 17 项断言(新增"博后视角:新决策卡默认证据 Tab"与集中式面板断言),17/17 通过;对齐 6/6、流式 11/11、按钮审计 58/58 全量回归通过。

### 9.2 输入框固定停靠(2026-08-16)

解决"每次都要往下翻好久才能输入"的问题:

- **桌面**:对话列内部滚动,输入框 `position: sticky; bottom: 0` 钉在列底——往上翻历史时输入框始终可见,滚到末尾时回到原位,无视觉跳变(列底内边距同步归零)。
- **平板/移动端**(≤940px,底部导航固定):咨询列改为 `overflow: visible`,输入框钉在**视口底部、导航上沿(46px)**;阅读线程时随时可输入,滚到决策卡面板区域后自然让位。
- 验证:`.composer-check.cjs` 5 项断言全部通过(桌面 sticky 位置/滚顶仍可见/滚底可见、移动端钉底 798px、不与导航重叠),并全量回归 capability 17/17、对齐 6/6、流式 11/11、按钮审计 58/58,0 控制台错误。

### 9.3 新手引导与角色面板收起(2026-08-16)

针对"新用户不知道点哪里、上方区域占空间"的反馈:

- **角色面板自动收起**:默认展开引导;选定角色后自动收起为一行(角色标签 + 一行注记 + 展开箭头),点箭头可再展开;会话开始后若从未选过角色也自动收起;清空对话回到欢迎态时重新展开(边沿检测,避免与选角收起互搏)。
- **欢迎态三步引导**:首页欢迎区改为「① 选择你的角色 → ② 点一张场景卡或直接提问 → ③ 在右侧查看可审计的决策卡」,新用户打开页面即知操作。
- **场景入口重组**:欢迎态隐藏五按钮场景条,改为 **3 张主场景卡**(实验方案设计 / 数据分析方法 / 论文写作支持,每张带一句说明)+ 2 个小号"机制演示"入口(追问 · 缺少 DV200、转接 · 证据冲突);会话开始后场景条才出现,并加"机制演示"分隔标签,演示按钮用虚线样式与主场景区分。
- 验证:`.capability-check.cjs` 重构为 26 项断言(移动端前置:欢迎卡/演示入口/无溢出;角色栏收起→展开、各角色模块、博后默认证据 Tab、场景条 3+2 与分隔、两大新场景、KPI),26/26 通过;流式 11/11、对齐 6/6、输入框停靠 5/5、按钮审计 58/58、135 单元测试全部通过,0 控制台错误。

### 9.4 角色镜头 + 快捷提问诚实化(2026-08-16)

针对"四个角色输出内容差不多、感觉不到侧重点"的评审反馈,落实"同一张决策卡,四套角色镜头":

- **角色镜头**:最新一条助手消息(聊天与决策卡气泡)开篇增加「XX 视角」标签 + 一句角色化导读——PI「结论先行:风险/预算/周期」、博后「参数/证据/复现」、研究生「概念/步骤」、企业「SLA/报价/合规」。**决策卡本体(标题/证据/风险/结论)四角色逐字段一致**,已用自动化断言锁定。
- **角色化动作区**(决策卡底部):PI=预约专家复核、博后=导出决策卡(复现)、研究生=术语卡(DV200/链特异性建库/PE150 三词条折叠)、企业=SLA 注记(18 天·4h 响应·数据不出域·授权进 CRM)。
- **快捷提问诚实化**:①场景模板不再写死,由当前项目事实拼装(`buildScenarioPrompt`,如"24 份、DV200 62%"实时进入问句);②一键注入的用户消息带「快捷提问」徽标,承认是模板而非手输;③模式 C——欢迎态场景卡一键演示(带徽标),会话开始后场景条改为**回填输入框待发送**,教用户自己提问。
- 验证:`.role-lens-check.cjs` 24 项断言(四角色徽标/镜头标签/侧重点关键词/卡片一致性/动作区/术语卡/场景条回填与事实拼装)全部通过;capability 26/26、流式 12/12、对齐 6/6、停靠 5/5、按钮审计 58/58、135 测试全绿,0 控制台错误。

### 9.5 角色栏钉顶(桌面端,2026-08-16)

评审反馈"四角色是否固定在页面上方"的结论:**桌面端固定,移动端不固定**。桌面端角色栏 `position: sticky; top: 0` 钉在咨询列顶(长对话中随时可切换角色镜头,收起态仅一行、成本低);上边距移入标题区避免钉住时跳动;≤940px 恢复 `static`(移动端已有顶栏/输入框/底部导航三层固定,不再叠加第四层)。验证:`.pin-check.cjs` 4 项断言(桌面 sticky 位置与钉顶坐标、移动端 static、无溢出)全部通过,全量回归绿。

> 附:QA 期间定位到环境级问题——Next 15 进程名为 `next-server`,此前按 `next start` 杀进程导致旧服务持库续读、测试偶发读到历史数据;已改为按端口(lsof)杀监听进程,所有回归脚本以干净库重跑全绿。

### 9.6 项目事实扩展(2026-08-16)

按"只加真实生效、不加装饰性"的原则扩展左侧项目事实:

- **物种下拉**(Homo sapiens / Mus musculus)与**研究目标下拉**(差异表达·通路富集 / 肿瘤微环境与免疫 / 其他):进入问句拼装(`buildScenarioPrompt`)与后端事实持久化(API zod 同步扩展),换物种/目标后场景条回填与决策卡问句即时变化。
- **事实元信息**:全部字段带"客户方案书 · 来源注记"(材料字段保留"第 2 页"具体出处),对齐大纲 3.2"每条信息带来源、版本、确认状态"。
- **待客户确认 chips**(配对设计 / 批次信息 / 组织量):灰色虚线 chips 展示"最小必要追问"的剩余空间,点击即发送对应追问(带快捷提问徽标),回答落地后 chip 变"已追问"。
- 专家工作台交接包的事实 chips 同步显示物种与目标。
- 未做:预算/周期可编辑(保持官方口径与转化钩子)、自由 key-value 自定义字段(后端 schema 重改、收益低)。
- 验证:`.facts-check.cjs` 11 项断言(下拉默认值/元信息 6 处/3 个 chips/切换后问句含新值/配对追问注入与切题回答/chip 状态)全部通过;全量回归 role-lens 24/24、capability 26/26、align 6/6、streaming 12/12、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.7 物种显示一致性修复(2026-08-16)

修复"物种选择是相反的"反馈,三处不一致一起根治:

1. **角色面板(PI 视角)chips 写死 Homo sapiens**:侧栏改了物种后角色面板仍显示旧值 → 改为读取实时事实(`facts.species / facts.goal`)。
2. **快捷提问使用旧确认值**:侧栏修改未确认时,点场景卡/发问题仍按旧的 `confirmedFacts` 拼问句,出现"选了小鼠、问句说人"的矛盾 → 改为**一律以当前界面事实为准**,修改后提问即视为对本次修改的确认(成功后徽标自动变"已确认")。
3. **物种下拉只有拉丁名**:改为双语选项「人 · Homo sapiens / 小鼠 · Mus musculus / 其他」,杜绝看反。

验证:切换物种后下拉、PI chips、决策卡问句三处一致(探针断言),并全量回归 facts 11/11、role-lens 24/24、capability 26/26、align 6/6、streaming 12/12、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.8 场景分组:四大核心场景 + 机制演示(2026-08-16)

澄清"方案设计等五个"的定位并重构:

- **核心场景 ×4**(对齐命题要求(1)四大场景,一一对应四类专业智能体):实验方案设计 / **测序平台选择(新拆分,原并入方案设计)** / 数据分析方法 / 论文写作支持——欢迎态 4 张大卡片,会话后场景条 4 个主按钮。
- **机制演示 ×2**(不是客户功能,是可信性机制):追问演示(条件缺失)、转接演示(证据冲突)——保持虚线样式 + "机制演示"分隔标签,与能力场景明确区隔。
- 新增 `platform-selection` 场景:意图关键词(平台/读长/数据量/Illumina/NovaSeq)识别,问句由事实拼装,检索落到官方平台口径证据(NV-SOP-MED-001/PLATFORM-051)。
- 验证:capability 升级为 27 项断言(4 张欢迎卡、场景条 4+2、平台选择卡片含平台/读长/数据量口径),全量回归 facts 11/11、role-lens 24/24、align 6/6、streaming 11/11、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.9 决策卡优化(2026-08-16)

按"让核心主张可感知"的原则优化决策卡三处(结构不动):

- **结论↔证据跳转**:主方案条目的证据编号改为可点击芯片,点击切到"证据"Tab、对应证据卡高亮(脉冲动画)并滚动定位——"有据才答"从文字变成可操作;手动切 Tab 自动清除高亮。
- **主方案视觉权重**:推荐条目加编号徽标(01/02)与证据芯片行,成为卡片主角。
- **风险刻度条**:状态卡内新增颜色分级的风险刻度(低=青/中=琥珀/高=红),宽度=风险分,一眼可见风险等级。
- 修复两个连带问题:①新"平台选择"场景的关键词正则过宽,把"方案设计"问句误判为平台场景——收紧为"推荐…测序平台/平台选择/NovaSeq/哪种平台"等强信号;②高亮滚动 `useEffect` 误放在提前返回之后导致 React #310(hook 数量不稳)——移到提前返回之前。
- 验证:`.card-check.cjs` 8 项断言(推荐条目编号与芯片、风险刻度宽度=分数、点芯片切 Tab+高亮、手动切 Tab 清高亮)全部通过;全量回归 role-lens 24/24、capability 27/27、align 6/6、facts 11/11、streaming 11/11、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.10 双栏可收起 + 按需生成决策卡(2026-08-16)

- **左栏收起**:标题行加「收起」按钮,收起后变为 52px 细条——诺禾致源 logo(悬停/点击展开)、「+」新建会话、「放大镜」搜索会话(弹出过滤列表);状态持久化(localStorage),咨询区实测 730→928px。
- **右栏决策卡收起**:面板左缘把手(ChevronRight)收起为 44px 细条(竖排"决策卡"标签 + 展开按钮);两栏同时收起时咨询区实测 928→1344px,给主界面留足空间。≤940px 隐藏把手(移动端布局不变)。
- **按需生成决策卡**:研究问题仍自动落卡(风险门/强制转接依赖它,不可交给用户);聊天型回复末尾新增「生成决策卡」按钮,由用户判断是否正式化(点击即基于已确认事实生成决策卡,带快捷提问徽标)。
- 修复:面板去掉 overflow:hidden 后 grid 项 min-height:auto 撑破行高(决策面板/咨询列均显式 min-height:0),输入框钉底与列内滚动恢复。
- 验证:`.collapse-check.cjs` 11 项断言(收起类名/细条三件套/咨询区变宽/悬停展开/搜索弹层/决策卡收起展开/正式化按钮)全部通过;全量回归 role-lens 24/24、capability 27/27、align 6/6、facts 11/11、card 8/8、streaming 12/12、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.11 专家工作台补强(2026-08-16)

补上"有据才答"在人工环节的三个缺口:

- **交接包证据审查区**(大纲 4.7 硬缺口):后端把 AI 检索证据写入 handoff(域类型 + graph 构造 + JSON 持久化),专家页新增证据审查板块——逐条展示引用/版本/适用边界/有效期/核验状态,可勾选"采用/排除"(排除项划线、不进入候选知识)。
- **门禁预览 + 跨页闭环**:专家修订区上方展示批准后五步流程(生成候选→Owner→NovaBench→人工批准→5% 灰度,高亮当前步);批准成功提示附「前往知识进化推进晋级 →」链接(/knowledge)。
- **队列轻量真实化**:删除"在线 4 人"假数字,值班卡显示真实计数(队列/待认领/已解决);新增四个状态分组(全部/待认领/我的待办/已解决)+ 风险排序;认领写入时间戳(claimedAt,退回时清除),队列与详情显示"SLA 剩余 N 分钟"。
- 未做(有意):结构化卡片字段编辑器与修订 diff(工程量大、演示边际收益小,留阶段二);客户侧退回可见性;批量操作与搜索。
- 验证:`.expert-check.cjs` 13 项断言(证据审查条数与核验状态/排除划线/门禁预览 5 步/队列分组/真实计数/认领时间与 SLA 剩余/批准提示与跳转链接)全部通过;全量回归 role-lens 24/24、capability 27/27、align 6/6、facts 11/11、card 8/8、collapse 11/11、streaming 12/12、composer 5/5、pin 4/4、click-audit 58/58,0 控制台错误。

### 9.12 专家工作台可交互性(2026-08-16)

针对"模块都无法点击和编辑"的反馈,明确可编辑边界并落地:

- **待决策项改为可勾选清单**(核心新增):专家逐项勾选解决,划线+计数"1/2 项已解决 · 可批准";批准时把已解决项并入修订文本,随候选知识进入知识进化页(跨页断言验证)。
- **编辑范围注记**:案例画布顶部一行说明——"可编辑:待决策项勾选 · 证据采用/排除 · 专家修订文本;交接包为 AI 审计记录,保持只读"。不加统一"编辑按钮":交接包本体(客户目标/转接原因/AI 已尝试)是审计链,可编辑会破坏"该转就转"的可信性。
- 证据审查(采用/排除)与专家修订(textarea)延续既有交互。
- 验证:`.expert-check.cjs` 升级为 17 项断言(新增清单勾选划线计数、编辑范围注记、候选知识并入已解决项),17/17 通过;click-audit 58/58、role-lens 24/24、capability 27/27、card 8/8、collapse 11/11 回归全绿。
### 9.13 知识进化模块重构(2026-08-16)

针对评审发现的五个断点落地(全部在副本内):

- **① 证据一致性**:`/api/expert-cases` 的 approve 增加 `evidenceIds` 入参,不再硬编码 `E-SOP-042/E-PMID-35361992`;专家工作台把交接包证据减去证据审查中排除的切片、按引文去重后随批准写入候选(排除项不再进入候选知识,缺省回退保持既有行为)。
- **② 多候选切换**:候选 id 由固定 `CK-260719-017` 改为按批准时间生成唯一 id(每次修订各成一个候选,互不覆盖);知识进化页传入全部候选,新增候选切换器(候选 id + 状态徽章),切换后门禁状态从该候选的审计轨迹复原(新增 `checksFrom`),刷新页面也不再丢失已通过的门。
- **③ 门禁角色归属**:四道门禁各标注责任人——知识 Owner 审核(rna-knowledge-owner)、NovaBench 回归(novabench)、发布经理人工批准(release-manager)、5% 灰度(release-manager + ops)。
- **④ 一键回滚**:灰度激活后出现「一键回滚」按钮——`/api/knowledge` 新增 `rollback` action,状态退回 owner-approved、`productionEligible=false`、生产索引即时移除(`removeDocument`);回滚后历史门禁结论保留、只需重走灰度,再次激活在审计轨迹中开启新一轮周期。
- **⑤ 审计轨迹可见**:候选卡新增审计时间线,逐条显示阶段/操作者(候选生成→…→灰度激活/已回滚);域层 `promoteCandidateKnowledge` 改为逐门追加记录而非整体替换,`candidate-created` 起点不再丢失。
- **附带修复**:编排图在检索前确保知识库已就绪(`chunkCount===0` 时播种),修复先访问专家工作台时交接包证据链为空的问题。

验证:`npm test` 136/136、`npx tsc --noEmit`、`npm run build` 全绿;新增 `.knowledge-check.cjs` 38 项断言(播种/切换器/证据排除/门禁角色/全链路晋级/一键回滚/重新灰度),38/38 通过、0 控制台错误。全量回归(每项独立清库重启):capability 27/27、streaming 11/11、align 6/6、composer 5/5、pin 4/4、role-lens 24/24、facts 11/11、card 8/8、collapse 11/11、expert 17/17、click-audit 58/58、smoke 通过,全部 0 控制台错误。
- **QA 脚本顺带修复**:`.smoke-check.cjs` 的场景条交互移到首次发送之后(场景条在会话开始后才渲染,清库后原位置必超时)。

### 9.14 候选影响面持久化(2026-08-16)

针对「NovaBench 回归跑完、刷新后最下面候选影响面又空了」的反馈:

- **报告落库**:`runNovaBench` 把完整报告(准确率、通过数、逐切片结果)随 eval_runs 持久化;新增 `getLatestBenchReport` 读取最近一次报告(兼容旧 records 无切片的行,读不到时安全返回空)。
- **服务端恢复**:知识进化页加载时读取最近报告作为 `initialBench` 传入组件,刷新/重进页面后切片表立即恢复,不再显示「尚未运行评测」。
- **兜底一键恢复**:若记录缺失(旧数据)而候选已通过 NovaBench,空态区出现「重跑评测恢复切片」按钮,一次点击即可恢复表格(复用真实 `/api/release-gates`,离线可用)。

验证:`npm test` 137/137(新增报告落库读取断言)、typecheck、build 全绿;`.knowledge-check.cjs` 39/39(新增「刷新后切片仍显示」断言);受影响回归子集 expert 17/17、click-audit 58/58、smoke 通过,全部 0 控制台错误。

### 9.15 运行评测模块重构(2026-08-16)

按评审结论(两处一起看)落地,运营评测页为主、知识进化页统一口径:

- **真实趋势替代假数据**:指标卡硬编码 sparkline 全部移除,改为从 eval_runs 运行历史生成的**真实趋势**(每张卡绑定对应指标:准确率/转接召回/引用有效率/P0;不足 2 次运行时显示"运行 ≥ 2 次后出现真实趋势");装饰性 CSS 雷达图删除,门禁列表成为主视觉。
- **五开关退化矩阵**:单一「模拟退化」开关扩展为五道硬门禁逐一注入(转接漏判/引用失效/自信错答/P0 缺陷/数据出域),可叠加;注入值、门禁失败、质量事件与 STOP 裁决全部带「模拟」标记,真实评测结果与运行历史保持干净(纯前端展示层注入)。
- **质量事件真实闭环**:新增 `gate_events` 表(与反馈质量事件 quality_events 分表)与 `/api/quality-events`(GET/POST open/resolve,写契约);门禁失守→事件落库(同门禁幂等)→人工关闭**必填关闭证据**→已关闭列表展示证据+时间,刷新后闭环状态持久。
- **运行历史与回看**:每次 NovaBench 以真实时间独立落库(修复固定 id 互相覆盖),运营页新增运行历史条(时间/准确率/PASS|STOP),可点选回看历史报告并「回到最新」;与知识进化页候选影响面共用同一份记录。
- **金标逐条可查**:运营页新增可展开的 9 条金标明细表(预期/实际/结果/违规引用,失败标红);知识进化页候选影响面下补失败切片说明(案例、预期→实际、违规引用)。
- 知识进化页口径同步:报告落库升级为完整案例(actual/违规引用),两处共用 `listBenchHistory`/`getLatestBenchReport`。

验证:`npm test` 140/140(新增 gate_events 闭环与历史 getter 单测)、typecheck、build 全绿;新增 `.operations-check.cjs` 37/37;`.click-audit.cjs` 运营段更新为退化开关+事件闭环语义(59/59);全量回归(每项独立清库重启):capability 27/27、streaming 12/12、align 6/6、composer 5/5、pin 4/4、role-lens 24/24、facts 11/11、card 8/8、collapse 11/11、expert 17/17、knowledge 39/39、operations 37/37、click-audit 59/59、smoke 通过,全部 0 控制台错误。
