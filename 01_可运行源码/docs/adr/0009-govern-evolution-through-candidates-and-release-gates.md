# 自进化只自动生成候选，不自动改写生产系统

可信 MVP 只允许在客户确认后更新当前项目事实，并从专家修改、失败咨询和检索缺口中自动生成候选知识或候选策略。任何候选项进入全局生产前必须通过知识 Owner 审核、NovaBench 回归测试、人工批准和小流量灰度；模型权重、智能体代码、工作流和正式 SOP 不得自行改写上线。

## Consequences

自动学习发生在候选发现层而非生产发布层；系统必须保存候选来源、审批轨迹、适用范围、版本和回滚路径，项目内即时记忆不得被误认为全局知识。
