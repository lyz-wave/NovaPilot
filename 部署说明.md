# NovaPilot 部署说明(点链接直接启动)

## 先说结论

- **GitHub Pages 不行**:NovaPilot 依赖 Node 服务端(内置 `node:sqlite` 本地数据库),Pages 只能托管纯静态文件。
- 仓库里已备好两种「点链接直接启动」方案:**Render 免费部署**(公开链接)与 **GitHub Codespaces**(登录 GitHub 一键启动)。

## 方案一:Render 免费一键部署(推荐,别人点链接直接用)

1. 打开一键部署链接(需先登录 render.com 并授权 GitHub):

    https://render.com/deploy?repo=https://github.com/lyz-wave/NovaPilot

2. 页面会读取仓库根目录的 `render.yaml`(Blueprint),确认服务名 novapilot、免费计划、健康检查 `/`,点 Create Web Service。
3. 部署完成(约 2-3 分钟)后,得到公开链接 `https://novapilot.onrender.com`,任何人可直接打开使用。

说明:

- 免费实例数据存 /tmp,重启/休眠后回到**演示初始态**(应用会自动播种演示数据,无需任何配置)。
- 空闲约 15 分钟后休眠,访问时数十秒自动唤醒。
- 需要数据持久化:在 Render 给服务挂载磁盘(付费),并把环境变量 `NOVAPILOT_DB_PATH` 改为 `/var/data/novapilot.db`。
- 镜像由根目录 `Dockerfile` 构建(Node 22 内置 node:sqlite,零原生依赖);本地可 `docker build -t novapilot .` 验证。

## 方案二:GitHub Codespaces(在仓库页点一下就启动)

1. 打开仓库 `github.com/lyz-wave/NovaPilot` → Code 按钮 → Codespaces 页签 → Create codespace on main。
2. 容器按 `.devcontainer/devcontainer.json` 自动 npm install + 构建 + npm start(端口 3210),完成后浏览器自动打开预览。
3. 需要公开给未登录用户时,把转发端口的可见性改为 Public(端口面板 → 右键 → Port Visibility)。

## 环境变量

| 变量 | 作用 | 默认 |
| --- | --- | --- |
| PORT | 服务端口 | 3000(next start 自动读取) |
| NOVAPILOT_DB_PATH | SQLite 数据库文件路径 | ./.data/novapilot.db |

## 本地运行(不变)

    npm install
    npm run build
    PORT=3210 npm start   # 打开 http://localhost:3210

## QA 验收脚本

本地清库后逐个运行(端口 3210):

    rm -rf .data
    node .knowledge-check.cjs   # 依此类推,共 14 个

脚本清单:capability、streaming、align、composer、pin、role-lens、facts、card、collapse、expert、knowledge、operations、click-audit、smoke。
